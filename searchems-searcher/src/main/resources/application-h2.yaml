spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:test
  jpa:
    show-sql: false
    properties:
      hibernate:
        jdbc:
          order_inserts: true
          batch_size: 500
searchems:
  endpoint:
    base-url: http://localhost:8080
